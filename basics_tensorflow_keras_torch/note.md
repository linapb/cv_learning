As you can see, Torch is much more verbose that Keras. It is because Torch gives much more flexibility for building custom models.

Therefore, Keras is much easier to start with for learning Deep Learning.